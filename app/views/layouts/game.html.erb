<!DOCTYPE html>
<html>
  <head>
    <%= render "layouts/head" %>

    <%= turbo_refreshes_with method: :morph, scroll: :preserve %>
    <%= yield :head %>
  </head>

  <body>
    <%= render "layouts/flash" %>

    <%= yield :top %>

    <main class="container-fluid questions">
      <%= yield %>
    </main>

    <%= yield :bottom %>
  </body>
</html>
