<!DOCTYPE html>
<html>
  <head>
    <%= render "layouts/head" %>

    <%= turbo_refreshes_with method: :morph, scroll: :preserve %>
    <%= yield :head %>
  </head>

  <body>
    <%= render "layouts/flash" %>

    <main class="container">
      <%= render "layouts/nav" %>
      <div class="page">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
